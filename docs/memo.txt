戦闘処理の設計をしています。
抜けている視点や拡張性などの懸念がないか、改修案をレビューしてください。

・現在の流れ
battle-scene切り替え
-gameState.combatPhase=command_waitingで攻撃ボタン表示
攻撃ボタン押下
-gameState.combatPhase=attackで攻撃ボタン非表示
-内部:turnOrder確定['player', 'enemy']
-forでturnOrder回す
--playerなら
---攻撃値生成
---アーマー減算してダメージ値生成
---hp減算
---ログ処理
---撃破判定
----お金加算
----gameState.combatPhase=resultで結果表示
--敵なら
---攻撃値生成
---アーマー減算してダメージ値生成
---hp減算
---ログ処理
---敗北判定
----敗北処理?覚えてないわ?
-ループ終了
-gameState.combatPhase=command_waitingで攻撃ボタン表示


改変案
各キャラに内部IDが必要。加入時にcrypto.randomUUIDでいいかも。
-playerは初期定義で生成（後々は加入処理時も生成）
-enemyは戦闘開始時のディープコピー時に生成して追加

getStatusも必要。状態異常の影響を受けたステータスの取り出し。
getStat(chara, property)
STATUS_MODIFIER = {weakness: { attack: { rate: -0.2 } }}みたいな

戦闘管理オブジェクトを作成し、攻撃ボタンによる進行ではなくターンシーケンス形式に変える。
要は攻撃ボタンで戦闘のすべてを進行するのではなく、裏側でターンシーケンスを回して操作キャラの番が来たらコマンド表示、っていう形にしたい。
-gameState.battle = null, を追加

戦闘開始処理実行battleInit
-gameState.battle=new Proxy({});で初期化
-gameState.battle.party=[proxy(player)];//取り急ぎ。playerにmemberを持たせるならそれになる
-gameState.battle.enemies=[proxy({
  ...event.enemy,//...enemyでデバッグのほうも
  currentHp: event.enemy.hp,//これ使ってないで
  id: crypto.randomUUID()
})];
-gameState.battle.turnOrder=[];中身は[...party, ...enemies].sort(() => Math.random() - 0.5).sort((a, b) => b.speed - a.speed);
-gameState.battle.currentChara=null;
-gameState.battle.turn=0;
-gameState.battle.phase=pending;でログパネル描画
-battleTurnStart();ターン開始処理実行
ターン開始処理
-gameState.battle.turn++;ターン加算
-gameState.battle.turnOrder=[]行動順決定
-（拡張予定：その他ターン開始時処理）
-行動処理開始
行動処理
-勝敗判定
--gameState.battle.partyが全員戦闘不能判定なら
---戦闘終了処理(win)実行
--gameState.battle.enemiesが全員戦闘不能判定なら
---戦闘終了処理(loose)実行
-行動順が残っていなければターン終了処理実行
-行動順を1つ取り出す=Actor
--gameState.battle.currentChara=Actor
--Actorが味方ならコマンド待機に移行
---gameState.battle.phase=command_waitingでコマンドパネル描画
----コマンド選択で行動実行処理実行
--Actorが敵ならランダムで行動を確定
---行動実行処理実行
行動実行処理（コマンド選択か敵の自動選択により発火）
-gameState.battle.phase=execでログや演出のパネル描画
-行動の効果を処理する（ここの処理は未確定、charaはgameStateからとれるがコマンド選択はどうとるか、コマンドデータの形式による）
--戦闘不能になったキャラをturnOrderから除去する
-gameState.battle.currentChara=null
-行動実行終了処理実行
行動実行終了処理
-状態異常処理実行（効果発揮や経過ターン減算）
-行動処理実行
ターン終了処理
-（拡張予定：その他ターン終了時処理）
-ターン開始処理実行
戦闘終了処理(win|loose)
-gameState.battle.phase=result(false=loose表示?)で結果パネル描画
-画面全体に透明当たり判定を設置
--押下イベントでgameState.battle初期化
--押下イベントでexplore-scene切り替え

gameState.battleの中身
{
  party: [],// 味方キャラProxy配列
  enemies: [],// 敵キャラProxy配列
  turnOrder: [],// 行動順のparty+enemies
  turn: 0,// ターン数
  phase: "",// string画面制御用フラグ[pending|command_waiting|exec|result]
  actor: null,// コマンド表示や行動実行用のキャラ保管
  pendingCommand: null// ターゲット選択や行動実行用のコマンドオブジェクト
}


コマンド設計
gameState.battle.pendingCommandを追加
pendingCommand{
  act:基本行動
  actDetail:アイテムやスキルの指定
  target:対象の指定。全体/ランダムの場合はnull
}

整理してみると対象選択ボタンの押下でコマンド処理開始でよさそう。
基本行動（⇒スキルかアイテムなら詳細行動）⇒対象
-コマンド？⇒こうげき⇒スライム：実行
-コマンド？⇒ぼうぎょ⇒じぶん：実行
-コマンド？⇒スキル⇒ヒール⇒味方2：実行
-コマンド？⇒アイテム(act=item&#26684;納)⇒爆弾(actDetail=3&#26684;納)⇒敵全体(target=null&#26684;納)：実行

選択しなおしはこう。
-戻るイベント：actDetailがあるならそれを削除、ないならactを削除
-コマンド？⇒アイテム(act=item&#26684;納)⇒爆弾(actDetail=3&#26684;納)⇒戻る(actDetail削除)⇒戻る(act削除)⇒アイテム⇒ポーション⇒味方1：実行

描画処理は？
-pendingCommand.actがない：基本行動4つ描画、イベントはdata-act="attack"の値をe.dataset.actで取得してpendingCommand.actに&#26684;納
-pendingCommand.actがskill：スキル選択を描画、イベントはdata-act-detail="heal"の値をe.dataset.actDetailで取得してpendingCommand.actDetailに&#26684;納
-pendingCommand.actがitem：アイテム選択を描画、イベントはdata-act-detail="1"の値をe.dataset.actDetailで取得してpendingCommand.actDetailに&#26684;納
-pendingCommand.actがattackまたはguard、もしくはpendingCommand.actDetailがある：対象選択を表示
--こうげきの場合、キャラが全体攻撃フラグを持っているなら全体選択肢のみdataなしで表示、普通は戦闘不能でないenemiesを表示
--ぼうぎょの場合、イベントハンドラ内でtarget=自身のidを設定、そのままコマンド処理実行
--スキルの場合、スキル一覧を表示
---そのスキルに設定された対象抽出タイプにて抽出メソッドを実行し対象を表示する。スキルの対象タイプがランダム/全体ならtarget=nullでコマンド処理実行、自身が対象ならtarget=自身のidでコマンド処理実行
--アイテムの場合、スキルと同様
-選択肢の選択イベント
--data-target="chara.id"の値をe.dataset.targetで取得してpendingCommand.targetに&#26684;納。またはe.dataset.targetがないならpendingCommand.targetをnullとする
--コマンド処理実行

イメージ
gameState.battle.pendingCommand = {type: "attack",targetId: "enemy_0"};
{ type: "skill", skillId: "power_strike", targetId: "enemy_0" }
{ type: "item",  itemId: "potion", targetId: "player_0" }
{ type: "guard" }


その他
・gameState.necessasryRender必要じゃね？proxyってない要素の更新時とか。
・画面設計しろ、サンプルDOMつくらせてもいいかもね


const gameState = {};

// ここから実装
const DEBUFF_STATUS_MODIFIERS = {
  weakness: { attack: { rate: -0.2 } },
  haste:    { speed:  { rate:  0.5 } },
  //poison:   {}, // ダメージ系はターン処理で扱うのでここには書かない
};

function getStatus(chara, property) {
  const base = chara[property];
  if (!chara.statusEffects || chara.statusEffects.length === 0) return base;

  let multiplier = 1.0;
  let flat = 0;

  for (const effect of chara.statusEffects) {
    const mod = DEBUFF_STATUS_MODIFIERS[effect.type]?.[property];
    if (!mod) continue;
    if (mod.rate)  multiplier += mod.rate; // 例: -0.2で20%減
    if (mod.flat)  flat += mod.flat;
  }

  return Math.max(0, Math.floor(base * multiplier) + flat);
}

//======================

function battleInit(enemies) {
  gameState.currentScene = 'battle_scene'; //TODO:正確でない
  gameState.battle = new Proxy({
    party: [player],// TODO: 取り急ぎ。playerにmemberを持たせるならそれになる
    enemies: [
      new Proxy({
        ...event.enemy,//...enemyでデバッグのほうも
        currentHp: event.enemy.hp,//これ使ってないで
        id: self.crypto.randomUUID()
      }, 例のアレ)],
    turnOrder: [],
    turn: 0,
    phase: "pending",//でログパネル描画
    actor: null,
    pendingCommand: new Proxy({}, 例のアレ),
  }, 例のアレ);
  battleTurnStart();//ターン開始処理実行
}

function battleTurnStart() {
  gameState.battle.turn++;
  // ランダム順にしてからソートする。速度同値がランダムになるように
  gameState.battle.turnOrder = [...gameState.battle.party, ...gameState.battle.enemies]
    .sort(() => Math.random() - 0.5)
    .sort((a, b) => getStatus(b, "speed") - getStatus(a, "speed"));
  //TODO: このへんで拡張予定：その他ターン開始時処理
  battleExecOrder();
}

function battleExecOrder() {
  // 勝敗判定
  // TODO: 戦闘不能判定をメソッド化したほうがいい ダメージ処理でマイナスにならないように
  if (gameState.battle.party.every(chara => chara.hp === 0)) {
    battleResult(false); //敗北。相打ちでも敗北が優先
    return;
  } else if (gameState.battle.enemies.every(chara => chara.hp === 0)) {
    battleResult(true); //勝利
    return;
  }

  // 全員行動済みならターン終了処理
  if (gameState.battle.turnOrder.length === 0) {
    battleTurnEnd();
    return;
  }

  // 味方ならコマンド選択を表示｜敵ならランダムに決定してコマンド実行処理
  gameState.battle.actor = gameState.battle.turnOrder.shift();
  if (gameState.battle.party.includes(gameState.battle.actor)) {
    gameState.battle.phase = "command_waiting";// コマンドパネル描画
  } else {
    // TODO ランダムに敵の行動を決定。取り急ぎパーティー先頭に攻撃
    gameState.battle.pendingCommand = {act: "attack", actDetail: null, targetId: gameState.battle.party[0].id};
    battleExecCommand();
  }
}

function battleExecCommand() {
  gameState.battle.phase = "exec";// ログや演出のパネル描画
  const cmd = gameState.battle.pendingCommand;
  gameState.battle.pendingCommand = new Proxy({}, 例のアレ);

  // 対象 all|random|enemy_all|enemy_random|ally_all|ally_random|id
  // targets = []に&#26684;納

  if (cmd.type === "attack") {
    //TODO: 攻撃処理
  } else if (cmd.type === "guard") {

  } else if (cmd.type === "skill") {

  } else if (cmd.type === "item") {

  } else {
    throw new Error('存在しないアクションです');
  }

  // 戦闘不能キャラを全取得してタイムラインから除く
  const down_chara_list = [...gameState.battle.party, ...gameState.battle.enemies]
    .filter((chara) => chara.hp === 0);
  gameState.battle.turnOrder = gameState.battle.turnOrder.filter(order =>
    !down_chara_list.some(chara => chara.id === order.id)
  );

  battleFinishCommand();
}

function battleFinishCommand() {
  gameState.battle.actor = null;
  // TODO: 状態異常処理実行
  // 次の行動につなぐ
  setTimeout(battleExecOrder, 1000);
}

function battleTurnEnd() {
  gameState.battle.phase = "pending";// ログパネル描画
  // TODO: ターン終了時処理
  setTimeout(battleTurnStart, 1000);
}

function battleResult(is_victory) {
  gameState.battle.phase = "result";// ログパネル描画
  if (is_victory) {
    // TODO: 勝利。探索に戻る
  } else {
    // TODO: 敗北。タイトル|探索終了して拠点に戻る
  }
  /*
-画面全体に透明当たり判定を設置
--押下イベントでgameState.battle初期化
--押下イベントでexplore-scene切り替え
  */
}


// ==========render

function renderBattle() {
  // pending | command_waiting | exec | result
  const phase = gameState.battle.phase;

  // 全非表示

  if (phase === "pending"
      || phase === "exec"
      || phase === "result") {
    // ログ表示
  } else if (phase === "command_waiting") {
    // コマンドパネル表示
    if (!gameState.battle.pendingCommand.act) {
      // 基本行動4つのパネル表示
    } else if (gameState.battle.pendingCommand.act === "skill" && !gameState.battle.pendingCommand.actDetail) {
      // スキル一覧を生成
      // TODO: イベントデリゲーションを調べて。親に代わりに登録しておく形。これでええやん
      /*こんな感じ
      const parent = document.getElementById('parent');

      parent.addEventListener('click', (e) => {
        const choice = e.target.closest('.clickable');
        if (!choice) return;

        const id = choice.dataset.id;
        call(id);
      });
      */
    } else if (gameState.battle.pendingCommand.act === "item" && !gameState.battle.pendingCommand.actDetail) {
      // アイテム一覧を生成
    } else if (gameState.battle.pendingCommand.act) {
      // 対象選択
      if (gameState.battle.pendingCommand.act === "attack") {
        // 健康な敵を抽出
        // イベントハンドラ登録
        // 対象を表示
      } else if (gameState.battle.pendingCommand.act === "skill" && gameState.battle.pendingCommand.actDetail) {
        // スキル一覧からスキル取得
        // スキルの対象抽出タイプからメソッドを呼び出して対象を抽出
        // イベントデリゲーションなのでハンドラ不要 ⇒ targetブロックに付与
        // 対象を表示
      } else if (gameState.battle.pendingCommand.act === "item" && gameState.battle.pendingCommand.actDetail) {
        // アイテム一覧からアイテム取得
        // アイテムの対象抽出タイプからメソッドを呼び出して対象を抽出
        // イベントハンドラ登なのでハンドラ不要 ⇒ targetブロックに付与
        // 対象を表示
      }
    }
  }
}
 

例えば、
主人公にのみ「捕縛スキル」を追加して、体力が残り20％(切り捨て,下限1)を切った相手に使用可能。
命中したら相手を「捕縛：1」の状態異常にする。捕縛状態はターン経過で減少しない。
「捕縛」使用後に3以上蓄積したら捕縛成功で戦闘からそのキャラを離脱させる。
捕縛状態のキャラは捕縛状態を1減らす「もがく」コマンドが利用可能になる。
（状態異常にターン減衰なしが必要）
 
こういった形なら、
主人公は「捕縛」を使い続けないといけない（ハンデを背負う）、
捕縛状態のキャラは「もがく」かHPを回復することで逃れられる、
「もがく」の無効化のためには睡眠や麻痺などの状態異常にして弱らせるのが有効である、
捕縛したい敵がいるならその敵以外はあらかじめ倒す、
弱い敵を捕縛したい場合は全体攻撃を控える、
といった形で戦略性が生まれそう。



あらすじ
帝国：戦の精霊信仰、王国：実りの精霊信仰、魔族：破壊の精霊（魔王）信仰
魔物使いもいる世界。
帝国民生まれ、王国との戦争の余波で故郷を追われる
数年後街で暮らして家を借りた、依頼で森へ、精霊の紋章を得る
紋章は生物を支配する力あり、敵を使役できるかもねってなる
依頼をこなしつつ仲間を増やす、ギルドから平原いってみたらって提案
平原へ、冒険者ランクアップで丘陵許可
丘陵クリアで山許可、山でやたら強い魔族と遭遇、敗北ですが見逃してもらう
報告、破壊の精霊復活の兆しかも、もっと鍛えろってことで海岸/森2/平原2/洞窟/洞窟2許可
ここからしばらく冒険タイム
・ランク上がった、お金あるなら屋敷買わない？⇒待機上限の拡大/庭機能解放
・闘技場とか出てみない？
・イベント：帝国貴族か王族との接触、王国内の腐敗の噂、帝国と王国の小競り合いの噂
ランクが2～3上がったら帝国と王国の紛争勃発
期間限定マップ：小戦場1⇒小戦場2⇒小戦場3と解放
小戦場3で敵主力を撃破し王国軍を撤退させる
何回か冒険後にイベント、帝国が電撃戦で土地を奪った
開拓地解放、危険の確認で向かう
開拓地で魔族と遭遇、撃破するも加減されていたと判明、逃げられる
力をつけるフェーズ
冒険者ランクを一定まで上げる、街に戻ると騒めき、王国の王都が占領され魔王が出現
魔王軍襲来
期間限定マップ：要塞防衛戦1⇒要塞防衛戦2⇒要塞防衛戦3と解放
要塞防衛戦3で勝利して魔族を撃滅
街へ戻ると逃げ延びた王国騎士から依頼、王女の救出
単発マップ：電撃戦で王女を王国のはずれから救出
帝国による攻撃が開始
王国：平原/火山/古戦場/森深層が解放
すべてクリア
王国軍残党が反攻、帝国と組んで魔族を攻撃
期間限定マップ：王都攻略戦1⇒王都攻略戦2⇒王都攻略戦3⇒王都攻略戦4と解放
決戦解放、魔王を使役/封印して終了
帝国と王国は和平を結んで大陸が平和になる
クリア
クリア後：新諸島発見、魔の次元発見、パンドラの洞窟発見


 
セーブ関連処理の見直し(Step4書き換え もっと手前でやっちゃってもいいかも）
サーバーやめて拠点でセーブダウンロードと読み込みをできるようにする。改造へのお気持ち抵抗として一応暗号化する。
タイトルにセーブはlocalstorageなので定期的にローカルバックアップしてねといれる。
 
数値計算（すべて切り捨て）
攻撃
ダイス数：速度 / 333 + 装備の値 + 1：下限1 999で3
ダイス面：攻撃の25% + 装備の値：下限1 999で250
修正値：器用 ^ 0.80 - 1 + 装備の値：下限0 999で250
素手だと3D250+250で最大で1000出せる
 
防御
防御値：(防御 + 体格/10) / 5 + 装備の値：下限0
回避値：速さ / 20 + (1001 - 体格 ^ 1.5) / 100 + 装備の値：下限0上限99
 
ダメージ計算
ダメージ：攻撃のダイスロール - 防御値：下限0
回避判定：1D100 < 防御側回避値 - 攻撃側回避値


TODO
・破棄画面
・トースト
・displayCurrentEvent

OK・アイテム使用イベント　イベントデリゲーション
OK・戻るボタン
OK・アイテム効果
OK・複数敵
OK・対象選択
OK・攻撃アイテム追加
OK・状態異常設計
OK・状態異常保持追加/ついでにskill_listも追加しといて
OK・状態異常解決追加
OK・ぼうぎょ処理
OK・戦闘終了処理追加
OK・敵の分離
0・スキル設計
0・スキル追加
・スキル描画、一旦ステータスの下にでも
0・スキル処理追加
・usableIn stateIdだけキャメってる
0・毒/睡眠/麻痺/スタンアイテム
0・毒/睡眠/麻痺/スタン
・盲目/捕縛/封印の処理
0・状態異常回復効果
0・状態異常アイコン
0・戦闘不能処理
0・戦闘不能はカードをマスク
0・復活処理
・CSS整理
・パネルのcssはidであてなよ　classいらんくない
0・ダメージ計算の改善
0・戦闘中のトースト

0・敵のドロップアイテム設定
0・戦闘終了時のドロップ判定追加
0・勝利時、各終了処理を実行し、リザルトポップアップを表示
・ポップアップ押下で探索に戻る

・職業設計
・職業追加
・キャラクリ職業設定画面追加
・Lv/職業ランク追加
・経験値設定
・経験値加算、職業経験値加算処理追加
・レベルアップ処理追加
・ランクアップ処理追加
・ランクアップ時の能力アップとスキル習得追加

0・levelとexp追加　初期化
0・エネミーにも追加
0・メニューに追加
0・戦闘カードに追加
0・戦闘終了時に獲得物ウィンドウ追加
0・exp加算メソッド追加
0・レベルアップ処理の追加（能力向上判定）
0・レベルアップ判定の追加
0・exp加算メソッド実行デバッグ追加

0・職業マスタ追加
0・currentJob追加
0・jobs追加
0・メニュー表示追加
0・エネミーにも追加
0・転職メソッド追加
0・デバッグ転職
0・キャラ作成時にジョブ選択
0・ランク経験値集計追加（＝敵の合計レベル＞自分レベルなら2、それ以外1）
0・ランクアップ時の能力アップ
0・ランクアップ時のスキル追加
0・ランクアップの描画
・デバッグ経験値/ランク経験値加算

・職業補正追加
・能力アップ補正アイテム追加

========================================

# 今週のタスク（平日5日・1日1?2時間）

> バグ修正を先に片付けて、後半は設計作業に集中する。

---

## 月曜：バグ修正①（数値系）
## 火曜：バグ修正②（機能系）

- [x] ランク経験値の計算式を `Math.floor(level * level * 1.5 + level * 10)` に変更
- [x] 経験値の計算式を `lv=1; n=lv-1; Math.floor((2.5 * n * (n + 1) * (2 * n + 1) / 6) + (10 * lv));` に変更
- [x] 成長率をHP75, MP50, その他30ベースに変更、職業成長補正は据え置きでもいいか
- [x] 短剣のダイス補正を修正（10d10+10 → 適切な値に。例: 0d3+2程度）
- [x] 上記変更後、装備変更で検&#35388;
- [x] ドロップアイテム取得不具合の修正（結果IDからアイテムマスタを再取得してバリデーション）
- [x] 物理スキルにキャラ自身の攻撃力加算を追加（`getStat(chara, "attack")` を加算する処理が抜けている箇所を特定して修正）
- [x] ランクアップ表示に「名前:職業」を追加（例:「ガレス:戦士がランクアップ！」）
- [x] 経験値加算デバッグ機能を追加
- [x] ランク経験値加算デバッグ機能を追加
- [x] 画面拡大無効化を追加。試す <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0">
- [x] 上記3点を戦闘・スキル使用・ランクアップそれぞれで動作確認

!!Extned!!
- [ ] 強くてニューゲームの導線を削除 strong-new-game-button
- [ ] 
- [ ] 
- [ ] 
- [ ] 

---

## 水曜：設計作業①（画面遷移・スクリーン定義）

- [ ] 最終的な画面一覧を確定する
  - タイトル / 初回キャラ作成 / 拠点 / 探索 / 戦闘 の5画面で足りるか検討
  - キャラ編成・倉庫・酒場・依頼板は「拠点のウィンドウ」か「独立した画面」か判断
- [ ] 画面遷移図を手書きまたはテキストで書く
  - 起動 → タイトル → 拠点 → 探索 → 戦闘 の主導線
  - 「はじめから」でセーブありの場合の確認ダイアログ分岐
  - 探索クリア・ゲームオーバー後の遷移
- [ ] 「画面」と「ウィンドウ」の判断基準（閉じたら元に戻るか）を各要素に適用して仕分け完了

---

## 木曜：設計作業②（マップ・探索の方向性）


- [ ] ダンジョンメーカー風マップ生成の方針を固める
  - マップタイル `[[ev, ba, re, bo]]` の各記号の定義（ev=イベント、ba=バトル、re=休憩、bo=ボス？）
  - 進行の3択（左・中央・右）とフロア/列の保存方法
  - フロア数・列数の上限など基本パラメータ
- [ ] マップデータ構造を仮決めする（メモのたたき台を元に）
  ```
  {
    id, name,
    floor_count,
    fix_floors: { 10: [...] },
    floor_gen_rules: {},
    events: [],
    monster_groups: [["id"...]]
  }
  ```
- [ ] セーブデータのexplore部分を設計する
  ```
  explore: {
    map_id, map: [[]], current_floor, current_line,
    is_event_clear, is_map_clear, is_map_gameover
  }
  ```
- [ ] 既存の探索処理（1人目ターゲット固定）のどこをどう変えるか影響範囲を書き出す

---

## 金曜：設計作業③（キャラ作成・引継ぎ）

- [ ] キャラ作成の新仕様を固める
  - ポイント制廃止
  - 職業選択 → その成長係数で能力補正
  - ユニットの基底オブジェクトから生成する構造
- [ ] 「はじめから」の引継ぎ仕様を確定する
  - 強くてニューゲーム廃止
  - 「つづきから」でクリア/エンドフラグありなら削除して0階リスタート、の挙動を整理
  - 引継ぎ不具合（ジョブ・レベル・装備）の原因を特定して修正方針だけ書く（実装は来週）
- [ ] 来週以降のタスクリストを更新する

---

## 来週以降に持ち越す作業

- スクリーン再設計の実装（タイトル改修・拠点追加）
- キャラ作成の実装改修
- 探索の正式改修（マップデータ分割・4人パーティ対応）
- ダンジョンメーカー風マップ生成の実装

導線
初起動： タイトル → 主人公作成 → 探索 → 戦闘 → 探索 → 拠点
新加入：拠点[酒場] → 酒場 → 加入候補表示+ロール → （加入選択） → 確認ダイアログ → （加入処理） → 酒場 → 探索
メニュー：拠点[メニュー] → メニュー[プレイヤータブ]
メニュー：[プレイヤータブ][ユニットタブ]
メニュー[プレイヤータブ]：[情報][手持ち][受諾依頼一覧]
メニュー[ユニットタブ]：[ステータス][スキル][装備][職歴]
編成：拠点[待機所] → 待機所[待機所タブ] → 編成(ドラッグ) → 編成の各処理 → 待機所 → 拠点
ステータス確認：待機所 → （ユニット長押し） → ステータス → 待機所
※メニューとステータスタブ表示を共通化できそう
捕縛ユニットの教育：待機所[捕虜タブ] → （教育処理） → 結果表示 → 捕虜 → 待機所[待機所タブ]
装備：ステータス[装備タブ] → （空きスロット選択） → アイテム一覧 → （装備処理） →ステータス[装備タブ]
※利用可能アイテムは探索中でないなら倉庫のものも含む。となるとID発番が必要
装備解除：ステータス[装備タブ] → （装備中スロット選択） → 確認ダイアログ → （解除処理） → ステータス[装備タブ]
転職：拠点[転職] → 転職ユニット一覧 → （ユニット選択） → 転職先一覧 → （選択） → 確認ダイアログ → （転職処理） → 転職ユニット一覧 → （戻る） → 拠点
倉庫：拠点[倉庫] → 倉庫[倉庫タブ] → （アイテム選択） → 詳細表示 → （手持ち/捨てる選択） → （処理） → 倉庫[倉庫タブ]
倉庫：倉庫[手持ちタブ] → （アイテム選択） → 詳細表示 → （倉庫/捨てる選択） → （処理） → 倉庫[手持ちタブ]
ショップ：拠点[ショップ] → ショップ → 拠点
購入：ショップ → （購入選択） → 購入一覧 → （アイテム選択） → 確認ダイアログ → （購入処理） → 購入一覧 → ショップ
※探索中でないなら手持ちがいっぱいのとき倉庫に追加
売却：ショップ → （売却選択） → アイテム一覧 → （アイテム選択） → 確認ダイアログ → （売却処理） → アイテム一覧 → ショップ
※探索中でないなら倉庫のアイテムも参照
依頼：拠点[依頼板] → 依頼板 → 拠点
依頼受諾：依頼板 → （依頼選択） → 依頼表示 → （受諾選択） → 確認ダイアログ → 受諾 → 依頼板
※全然関係ないけどプレイ日数は必要かも、探索開始で場所ごとに+N日？
探索：拠点[探索] → 探索マップ選択 → （戻る） → 拠点
探索：探索マップ選択 → （マップ選択） → 出発確認 → （出発） → 探索
探索中：探索開始マス実行 → 3択選択肢表示 → （選択） → イベント実行 → ゴールでないなら3択選択肢表示
探索結果：ゴール/全滅 → 探索結果 → 拠点
探索中メニュー：メニューに撤退ボタンを追加のみ
アイテム破棄：アイテム獲得 → （上限枠超過） → アイテム破棄 → （選択） → 確認ダイアログ → 獲得



======================================================================
# 暁の探訪者 ? プロジェクト現況まとめ

> 作成日: 2026-06-15

---

## 1. 実装済み機能

### 画面
| 画面 | 状態 | 備考 |
|------|------|------|
| タイトル | ?? 改修必要 | 強くてニューゲーム廃止予定 |
| キャラ作成 | ?? 改修必要 | 初回キャラ作成に統合予定 |
| 探索 | ?? 改修必要 | パーティ4人対応が必要 |
| 戦闘 | ? 正式実装済み | |
| 探索クリア | ?? 改修必要 | |
| 探索ゲームオーバー | ?? 改修必要 | |

### ウィンドウ
| ウィンドウ | 状態 | 備考 |
|-----------|------|------|
| メニュー | ?? 改修必要 | 1人構成のまま。4人対応が必要 |
| 超過アイテム破棄 | ?? 改修必要 | |

---

## 2. 最終構成（目標）

### 画面
| 画面 | 説明 |
|------|------|
| タイトル | はじめから / つづきから の2択 |
| 初回キャラ作成 | 初回起動時のみ。主人公の作成 |
| 拠点 | ゲームのハブ。探索・ショップ・屋敷・転職など |
| キャラ編成 | 同行パーティの編成 |
| 倉庫 | アイテム管理 |
| 酒場 | 仲間の加入・解雇 |
| 依頼板 | クエスト受注・報告 |
| 探索 | マップを進む。イベント・戦闘が発生 |
| 戦闘 | ターン制バトル ? |

### ウィンドウ（画面上に重なるモーダル）
| ウィンドウ | 説明 |
|-----------|------|
| メニュー | 探索中のプレイヤー・ユニット情報確認 |
| 超過アイテム破棄 | アイテム枠超過時の破棄選択 |
| 転職 | ユニットの職業変更 |
| イベント会話 | イベントの選択肢表示 |
| デバッグ | 開発用。任意イベント・戦闘・アイテム取得 |

---

## 3. 画面とウィンドウの定義（暫定）

> 現状「画面」と「ウィンドウ」の役割が混在しているため、ここで定義を固める。

| 種別 | 定義 | 実装上の扱い |
|------|------|------------|
| **画面** | ゲームの主要な状態。1度に1つだけ表示される | `gameState.currentPage` で制御 |
| **ウィンドウ** | 画面の上に重なるモーダル。画面を切り替えずに補助情報・操作を提供する | `gameState.openModal` で制御。画面とは独立 |

**判断基準**: 「それを閉じたら元の画面に戻るか」→ ウィンドウ。「それ自体が独立した状態か」→ 画面。

---

## 4. 現在の主な技術的負債

| 項目 | 内容 |
|------|------|
| パーティ人数 | 1人固定 → 最大4人に変更済みだが、探索イベント処理が1人目ターゲット固定のまま |
| メニュー | 1人構成のまま。プレイヤーとユニットの出し分けが未対応 |
| 引継ぎ処理 | ジョブ・レベル・装備追加による「はじめから」「強くてニューゲーム」の不具合 |

---

## 5. メニューウィンドウ案

2層タブ構造。

```
メニュー
├─ プレイヤータブ
│   ├─ 汎用情報（所持金・プレイ時間など）
│   ├─ 所持アイテム
│   └─ 受諾依頼一覧
└─ ユニットタブ（1～4人）
    ├─ ステータス
    ├─ スキル
    ├─ 装備
    └─ 職歴
```

---

## 6. 喫緊の課題（優先順）

1. **画面遷移図の作成** ? プレイヤーの導線を定義。考慮漏れの洗い出し
2. **画面/ウィンドウ定義の確定** ? 上記3節の定義を全画面・全ウィンドウに適用
3. **探索の改修** ? マップデータとイベント定義の分割。4人パーティ対応
4. **拠点画面の追加** ? タイトルを「はじめから」「つづきから」の2択に変更。強くてニューゲーム廃止